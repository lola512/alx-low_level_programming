#include <stdio.h>

/**
 * main - print 16 numbers
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int i;

	for (i = 0; i < 11; i++)
	{
		putchar(i);
	}
	for (i = 0; i < 7; i++)
	{
		putchar(i)
	}
	putchar('\n');
	return (0);
}
